{
  "name": "arrayreduce-example",
  "version": "1.0.0",
  "description": "Unlike most Array iteration methods, which return an array of values, the reduce() method will reduce an array to a single value. In this example, the method will return the longest item in the array.",
  "main": "index.html",
  "scripts": {
    "start": "parcel index.html --open",
    "build": "parcel build index.html"
  },
  "dependencies": {},
  "devDependencies": {
    "@babel/core": "7.2.0",
    "parcel-bundler": "^1.6.1"
  },
  "keywords": ["javascript", "arrays", "iteration", "Array iteration", "array reduce", "array reducer", "reducer"]
}